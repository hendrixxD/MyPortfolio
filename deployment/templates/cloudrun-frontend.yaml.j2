# Generated by deployment.py - DO NOT EDIT MANUALLY
# Cloud Run Frontend Service Configuration
# Generated at: {{ config._generated_at }}

apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: portfolio-frontend
  labels:
    cloud.googleapis.com/location: {{ config.GCP_REGION }}
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: '{{ config.get("MIN_INSTANCES", "1") }}'
        autoscaling.knative.dev/maxScale: '{{ config.get("MAX_INSTANCES", "10") }}'
        run.googleapis.com/cpu-throttling: '{{ config.get("CPU_THROTTLING", "false") }}'
        run.googleapis.com/execution-environment: gen2
    spec:
      serviceAccountName: cloud-run-sa@{{ config.GCP_PROJECT_ID }}.iam.gserviceaccount.com
      containerConcurrency: {{ config.get("CONTAINER_CONCURRENCY", "80") }}
      timeoutSeconds: {{ config.get("TIMEOUT_SECONDS", "300") }}
      containers:
      - image: {{ config.GCP_REGION }}-docker.pkg.dev/{{ config.GCP_PROJECT_ID }}/portfolio/frontend:latest
        ports:
        - containerPort: 8080
          name: http1
        env:
        - name: NODE_ENV
          value: "production"
        - name: NEXT_PUBLIC_API_URL
          value: "{{ config.get('NEXT_PUBLIC_API_URL', '') }}"
        - name: NEXT_PUBLIC_SITE_URL
          value: "{{ config.get('NEXT_PUBLIC_SITE_URL', '') }}"
        {% if config.get('NEXT_PUBLIC_SENTRY_DSN') -%}
        - name: NEXT_PUBLIC_SENTRY_DSN
          value: "{{ config.NEXT_PUBLIC_SENTRY_DSN }}"
        {%- endif %}
        {% if config.get('NEXT_PUBLIC_GA_ID') -%}
        - name: NEXT_PUBLIC_GA_ID
          value: "{{ config.NEXT_PUBLIC_GA_ID }}"
        {%- endif %}
        {% if config.get('NEXT_PUBLIC_PLAUSIBLE_DOMAIN') -%}
        - name: NEXT_PUBLIC_PLAUSIBLE_DOMAIN
          value: "{{ config.NEXT_PUBLIC_PLAUSIBLE_DOMAIN }}"
        {%- endif %}
        resources:
          limits:
            cpu: '{{ config.get("FRONTEND_CPU", "2") }}'
            memory: {{ config.get("FRONTEND_MEMORY", "1Gi") }}
          requests:
            cpu: '{{ config.get("FRONTEND_CPU_REQUEST", "1") }}'
            memory: {{ config.get("FRONTEND_MEMORY_REQUEST", "512Mi") }}
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 0
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 10
